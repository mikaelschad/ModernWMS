export const config = {
    // Threshold for switching from dropdown to search modal
    // If items.length <= threshold, show dropdown
    // If items.length > threshold, show search modal
    entitySelectorThreshold: 10,

    // API base URL
    apiBaseUrl: 'http://localhost:5017',
}
